{
  "name": "hello-graphql",
  "version": "1.0.0",
  "description": "Try GraphQL Server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --transpile-only --no-notify --exit-child src/index.ts",
    "postinstall": "tsc",
    "start": "node dist/index.js",
    "generate": "ts-node --transpile-only src/schema.ts"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^3.10.0",
    "apollo-server": "^3.6.3",
    "apollo-server-express": "^3.6.3",
    "nexus": "^1.2.0"
  },
  "devDependencies": {
    "graphql": "^15.8.0",
    "node-ts": "^5.1.2",
    "prisma": "^3.10.0",
    "ts-node": "^10.5.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.5"
  }
}
