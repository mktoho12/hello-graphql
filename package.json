{
  "name": "hello-graphql",
  "version": "1.0.0",
  "description": "Try GraphQL Server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --transpile-only --no-notify --exit-child src/index.ts",
    "build": "tsc && npx prisma migrate deploy",
    "start": "node dist/index.js",
    "generate": "ts-node --transpile-only src/schema.ts",
    "script": "ts-node --transpile-only src/script.ts"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^3.10.0",
    "apollo-server": "^3.6.3",
    "apollo-server-express": "^3.6.3",
    "bcrypt": "^5.0.1",
    "dotenv": "^16.0.0",
    "jsonwebtoken": "^8.5.1",
    "nexus": "^1.2.0"
  },
  "devDependencies": {
    "@apollo/rover": "^0.4.3",
    "@types/bcrypt": "^5.0.0",
    "@types/jsonwebtoken": "^8.5.8",
    "graphql": "^15.8.0",
    "node-ts": "^5.1.2",
    "prisma": "^3.10.0",
    "ts-node": "^10.5.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.5"
  }
}
